@page "/"
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Authorization @*This package NEEDS to be here, will not work without, despite Visual Studio saying that it isn't used*@
@attribute [Authorize]

<AuthorizeView>
    <h1>Instrumentbræt</h1>
    <hr/>
    <p>Velkommen til dit instrumentbræt, @context.User.Identity.Name!</p>
</AuthorizeView>